// Button Constants
export { variants, sizes, spinnerSizes } from './button';

// About Constants
export { features } from './about';

// FAQ Constants
export { faqs } from './faq';

// Navigation Constants
export { mainNavLinks, profileNavigation } from './navigation';

// Config Constants
export { config } from './config';

// Theme Constants
export { colors } from './theme';

// Input constants
export const inputSizes = {
	sm: 'px-3 py-1.5 text-xs',
	md: 'px-4 py-2.5 text-sm',
	lg: 'px-5 py-3 text-base'
};

export const inputVariants = {
	default: 'bg-gray-50 dark:bg-dark-card border-gray-200 dark:border-dark-border',
	filled: 'bg-gray-100 dark:bg-dark-hover border-transparent',
	outline: 'bg-transparent border-gray-300 dark:border-gray-600'
};
